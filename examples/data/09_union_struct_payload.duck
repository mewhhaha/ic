const { struct } = comptime import "duck:prelude" ()

type User = struct { .id = Int, .score = Int }
type Result = | .ok = Int | .err = Text

let user: User = [1, 42]
let result: Result = Result.ok(user.score)

if let .ok(score) = result {
  score
} else {
  0
}
