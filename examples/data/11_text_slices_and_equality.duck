const { append, length, slice } = import "duck:prelude/runtime" ()

let word = "lambda"
let head = slice(word, 0, 3)
let tail = slice(word, 3, length(word))
let rebuilt = append(head, tail)

if rebuilt == word {
  42
} else {
  0
}
