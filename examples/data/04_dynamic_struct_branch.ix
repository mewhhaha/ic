module (!init: Init) where

declare effect Input {
  flag: () => I32
}

declare Init { input: Input }

type Point = (.x = Int, .y = Int)

flag <- Input.flag()
let point: Point = if flag {
  (.x = 40, .y = 2)
} else {
  (.x = 20, .y = 22)
}

let result: I32 = point.x + point.y
return { result }
