type User = (.id = Int, .score = Int)
type Result = .ok = Int | .err = Text

let user: User = (.id = 1, .score = 42)
let result: Result = Result.ok(user.score)

if let .ok(score) = result {
  score
} else {
  0
}
