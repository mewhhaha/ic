const pair = () => {
  return [7, 40]
}

let _ = 1
const _ = 2
const [_, value] = pair()
let add_two = (_, number) => number + 2

type Option = | .some = I32 | .none

let choice = Option.some(99)
let result = if let .some(_) = choice {
  add_two(100, value)
} else {
  0
}

result
