const pair = () => {
  return [7, 40]
}

let _ = 1
const _ = 2
const [_, value] = pair()
let add_two = (_, number) => number + 2

type Option = | `Some I32 | `None Unit

let choice = `Some (99)
let result = if let `Some _ = choice {
  add_two(100, value)
} else {
  0
}

result
