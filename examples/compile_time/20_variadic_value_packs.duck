const sum = (const ...values) => comptime {
  let total = 0;

  for value in values {
    total = total + value
  }

  total
};

let first = match (2, 99) {
  | (value, ...remaining) => value
  | _ => 0
};

sum(19, 20, 1) + first
