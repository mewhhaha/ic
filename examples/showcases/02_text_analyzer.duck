const { append, length, slice } = comptime import "duck:prelude/runtime" ()

let source = "lambda"
let head = slice(source, 0, 3)
let tail = slice(source, 3, length(source))
let rebuilt = append(head, tail)
let checksum = 0

for byte in rebuilt {
  checksum = checksum + byte
}

if rebuilt == source {
  checksum - 567
} else {
  0
}
