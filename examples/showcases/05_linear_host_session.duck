module (!init: Init) where

const _ = comptime import "duck:prelude/runtime" ()

declare effect Host {
  print: (&Text) => I32
}

declare Init { host: Host }

let message = scratch {
  let temporary: Text = "hel" <> "lo"
  freeze temporary
}

result <- Host.print(&message)
return { .result = result }
