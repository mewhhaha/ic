module (!init: Init) where

const { get } = import "duck:prelude/runtime" ()

declare effect Input {
  index: () => I32
}

declare Init { input: Input }

index <- Input.index()
let result: I32 = get("a", index)
return { .result = result }
